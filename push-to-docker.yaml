- name: Build and push Docker image
  run: |
    docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}azeimis/ndarbas1:latest .
    docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} -p ${{ secrets.DOCKER_HUB_PASSWORD }}
    docker push ${{ secrets.DOCKER_HUB_USERNAME }}azeimis/ndarbas1:latest
